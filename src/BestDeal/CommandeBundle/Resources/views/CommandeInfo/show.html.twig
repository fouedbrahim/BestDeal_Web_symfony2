{% extends '::base.html.twig' %}

{% block body -%}
    <h1>CommandeInfo</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Qte</th>
                <td>{{ entity.qte }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('commandeinfo') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('commandeinfo_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
